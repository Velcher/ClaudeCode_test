# Claude Code 实验项目

## 项目概述
初次体验 Claude Code 的实验性项目，探索 Claude Code 的能力边界。

## 技术栈
- verilog

## 项目结构
按功能模块组织模块和包。

## 常用命令
- 构建/安装依赖：`pip install -e .` 或 `pip install -r requirements.txt`
- 运行：`python -m <module>`

## 代码风格
- 遵循 PEP 8 标准
- 使用标准 Python 命名约定（snake_case 变量/函数、PascalCase 类）
- 保持简洁，避免不必要的注释

## Git 权限
- 所有 git 命令可以直接执行（无需逐一确认），包括 add、push、status、diff、log 等
- **例外**：git commit 提交前需要用户同意
- 同样适用于 gh 相关命令（如 gh pr create 等）

## 注意事项
- 不要擅自修改配置文件，修改前先询问
